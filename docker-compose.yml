version: '3.8'

services:
  sibh-api:
    build: .
    ports:
      - "${PORT}:${PORT}"
    environment:
      - PORT=${PORT}
networks:
  sibhapi-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.66.0.0/16